function maxScoreSightseeingPair(values: number[]) : number {
  var maxScore : number = Number.MIN_SAFE_INTEGER;
  let pairs : number[] = new Array<number, number>(values.length);
  // pair calc = values[i] + values[j] + i - j;
};
